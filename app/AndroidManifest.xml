<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sanacorapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="._load_water_control"></activity>
        <activity android:name="._dash" />
        <activity android:name="._motor_control" />
        <activity android:name=".sampsampsamp" />
        <activity android:name="._list_of_reading" />
        <activity android:name="._data_aqc" />
        <activity android:name="._login" />
        <activity android:name="._config" />
        <activity android:name=".LoadingScreen.loadScreen" />
        <activity android:name=".samplingstring" />
        <activity android:name="._pond_control">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>